{
  "name": "tierra-magica-foundry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sistema Tierra Mágica para Foundry VTT",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/tierra-magica.mjs && node --check scripts/documents/actor.mjs && node --check scripts/sheets/actor-sheet.mjs && node --check scripts/sheets/item-sheet.mjs",
    "validate": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
